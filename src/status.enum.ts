export enum SubStatus {
  ENABLED = "enable",
  ACTIVE = "active",
  CANCELLED = "cancelled",
  EXPIRED = "expired",
  PENDING = "pending"
}

export enum UserStatus {
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
}

